apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:stable
      tolerations:
        - key: "prod"
          operator: "Equal"
          effect: "NoSchedule"
          value: "true"
